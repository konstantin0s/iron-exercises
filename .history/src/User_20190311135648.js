import React from 'react'
import { UsersList } from './UsersList';

export default function User() {
  render() {
    const user = users[0];
    return(
      <div>
        <UsersList 
        firstName = {product.id}
        />
      </div>
    );
  }
}
